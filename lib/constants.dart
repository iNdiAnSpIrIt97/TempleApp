import 'package:flutter/material.dart';

class AppColors {
  // Dark Theme Colors
  static const Color darkBackground = Colors.black;
  static const Color darkIcons = Color(0xFFFFD700); // Gold
  static const Color darkText = Color.fromARGB(255, 255, 254, 253); // Cream
  static const Color darkOther = Colors.orange;
  static const Color darkCardBackground = Color(0xFF1A1A1A); // Dark Grey
  static const Color darkAccent = Color(0xFFFFA500); // Orange Accent
  static const Color darkShadow = Color(0x80000000); // Black with opacity

  // Light Theme Colors
  static const Color lightBackground = Color(0xFFFFF5E1); // Cream
  static const Color lightIcons = Colors.orange;
  static const Color lightText = Colors.black;
  static const Color lightOther = Colors.red;
  static const Color lightCardBackground = Colors.white;
  static const Color lightAccent = Color(0xFFFF5722); // Deep Orange
  static const Color lightShadow = Color(0x1A000000); // Black with low opacity
}
